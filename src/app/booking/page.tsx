import BookService from '@/components/view/BookService/BookService';
import React from 'react';

const page = () => {
    return (
        <div className='-mt-16'>
         <BookService />
        </div>
    );
};

export default page;